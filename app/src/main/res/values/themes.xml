<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Enwallet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.MentorGram" parent="Base.Theme.Enwallet">
        <item name="android:fontFamily">@font/teachers</item>
    </style>

    <style name="tvTitleStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">15dp</item>

    </style>

    <style name="Theme.Enwallet" parent="Base.Theme.Enwallet" />
</resources>